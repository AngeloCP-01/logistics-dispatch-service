# logistics-dispatch-service

Driver assignment and availability tracking for the AI Logistics Platform. Neon Postgres + Redis. Consumes `order.created`, publishes `dispatch.driver.assigned`.

> **Status:** scaffolded — implementation pending Phase 4 (see `CLAUDE.md`).
